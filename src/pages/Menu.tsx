import {StyleSheet, Text, View} from 'react-native';
import React from 'react';
import MenuButton from '../components/menuComponents/MenuButton';

const Menu = () => {
  return (
    <View>
      {/* <MenuButton title="Todo App" /> */}
      <Text>Menu</Text>
    </View>
  );
};

export default Menu;

const styles = StyleSheet.create({});
